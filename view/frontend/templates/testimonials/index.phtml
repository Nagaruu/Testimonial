
<ol class="testimonial-blog">
        <?php
        foreach ($block->getAll() as $item) {
            echo "<li>";
            echo "<p class='testimonials-title'>What our clients are saying</p>";
            ?>
            <a class='testimonials-button' href="<?php echo $block->getCreate(); ?>"></a>
            <?php
            echo "<img src=".$block->getBaseUrl()."pub/media/testimonials/index/".$item->getImages().">";
            echo "<p class='testimonials-message'>".$item->getMessage()."</p>";
            echo "<p class='customer-name'>".$block->getNameCustomer($item->getCustomerid())."</p>";
            // echo "<a href=".$block->getBaseUrl()."testimonials/index/edit/id/".$item->getId().">Edit</a>";
            echo "</li>";
        }
        ?>
</ol>
<script>
    require([
        'jquery',
        'slick'
    ], function ($) {
        $(document).ready(function () {
            $(".testimonial-blog").slick({
                autoplay: true,
                slidesToShow: 1,
                slidesToScroll: 1,
                autoplay: true,
                autoplaySpeed: 2000,
            });
        });
    });
</script>